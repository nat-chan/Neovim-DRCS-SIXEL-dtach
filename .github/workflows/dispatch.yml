name: hello

on:
  repository_dispatch:
  schedule:
    - cron: "*/10 * * * *"
jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - name: date
        run: date
# vim: tabstop=2 shiftwidth=2 expandtab
